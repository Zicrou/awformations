<!DOCTYPE html>
<html>
  <head>
    <title>Awformation</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "headers", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "shared/header" %>
    <p class="notice" style="z-index:99;"><%= notice %></p>
    <p class="alert" style="z-index:99;"><%= alert %></p>
    <%#= render "cart/cart" if @render_cart %>
    <%= yield %>
    <%= render "shared/footer" %>
  </body>
</html>
