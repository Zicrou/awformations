<div class="" style="">
  <p style="color: green"><%= notice %></p>

  <h1>
    <%= link_to "New cour", new_cour_path, class:"btn btn-outline-primary" %>
  </h1>
  <div id="cours">
    <main style="">
      <div class="container marketing" style="margin-top:2%;">
        <% @cours.each do |cour| %>
        <hr class="featurette-divider">
          <div class="row featurette mb-4">
            <div class="col-md-7">
              <h2 class="featurette-heading fw-normal lh-1"><%= cour.title %><span class="text-body-secondary"> Prix: €80.00</span></h2>
              <p class="lead"><%= cour.description %></p>
              <%= link_to "Voir +", cour, class:"btn btn-warning mt-auto m-2", style:"" %> 
              <%= link_to "Panier", "#", class:"btn btn-danger mt-auto m-2", style:"" %> 
            </div>
            <div class="col-md-5">
              <%= image_tag cour.thumbnail, class:"card-img-top", alt:"..." if cour.thumbnail.present? %>
            </div>
          </div>
        <% end %>
        <hr class="featurette-divider">
      </div>
  </div>
</div>
